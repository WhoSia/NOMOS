[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nomos-research"
version = "0.2.0"
description = "Typed audit kernel for NOMOS relational person-judgment research"
requires-python = ">=3.12"
readme = "README.md"

[project.scripts]
nomos = "nomos.__main__:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
